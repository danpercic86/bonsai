# P69c — Draft divergence feedback on numeric settings (UI contract) — SUPERSEDED

**Folded into `docs/contracts/P69-settings-ui.md` §13 by P69l (2026-08-20).** That section is now the
single binding spec for the `NumberSlider` out-of-range/blank draft affordance — including the timing
split (above-max warns on the causing keystroke; below-min, blank and non-integer warn only after
600 ms idle) — and `docs/contracts/ui-reference.md` §12.3.4 is its durable extract.

This stub is kept only because `TODO.md` entries and commit messages reference this path; a dangling
reference is worse than a pointer. Do not add content here.
