node_modules/
dist/
/target/
src-tauri/target/
crates/*/target/
src-tauri/gen/schemas/
*.log
.DS_Store
.idea/
# P42: updater signing keys — DEV verification key + password. NEVER commit.
# The private key + password go into CI as TAURI_SIGNING_PRIVATE_KEY /
# TAURI_SIGNING_PRIVATE_KEY_PASSWORD. Only the PUBLIC key is committed
# (tauri.conf.json plugins.updater.pubkey).
.tauri/
